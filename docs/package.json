{
  "name": "aligncv-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run start\" \"npm --prefix backend run start\"",
    "format": "prettier -w .",
    "format:check": "prettier -c ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  }
}