
# 21 — Templates, Tokens & Thumbnails

- 30 套模板（JR 20 + LaTeX 10）的 **Token**（字体、字号、行高、主色、布局双/单栏…）
- `tokensToCssVars()` 应用到预览容器；切换模板不改变内容
- 缩略图：`assets/templates/thumbnails/<id>.svg`（与 Token 对齐，单行工具条）
- 行业 → 默认模板推荐：`recommendations/industry-defaults.json`
