import { Injectable } from '@angular/core'; import { Observable, of } from 'rxjs'; import { Resume } from '../models/resume';
@Injectable({ providedIn: 'root' }) export class ResumeApi { getResume(id:string):Observable<Resume>{ return of({ id, title:'Demo', locale:'en-CA', blocks:[{id:'summary', type:'summary', data:{text:'Summary here'}},{id:'experience', type:'experience', data:{text:'• Migrated reports to Angular\n• Cut render time by 35%\n• Automated regression suite'}}] }); } saveResume(resume:Resume):Observable<Resume>{ return of({ ...resume, updatedAt:new Date().toISOString() }); } }
