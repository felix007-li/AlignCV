# AlignCV 精选12个模版

从原来的30个模版精简到12个最实用的模版，类似CVWizard的设计理念。

## 📋 精选模版列表

### 基础/经典 (4个)

1. **jr-modern** - Modern
   - 样式：现代简约、单栏
   - 适用：软件工程、产品经理、通用

2. **jr-even** - Even
   - 样式：平衡布局、单色
   - 适用：软件工程、通用

3. **jr-compact** - Compact
   - 样式：紧凑单页、极简
   - 适用：学生、应届生

4. **lt-moderncv** - ModernCV
   - 样式：经典专业、标题块
   - 适用：通用、学术、工程

### 创意/设计 (3个)

5. **jr-elegant** - Elegant
   - 样式：卡片式、响应式
   - 适用：产品设计、市场营销

6. **lt-altacv** - AltaCV
   - 样式：双栏、药丸式标题
   - 适用：软件工程、设计

7. **lt-friggeri** - Friggeri
   - 样式：双栏、彩色分区
   - 适用：科研、数据分析

### 技术/工程 (3个)

8. **jr-dev-ats** - Developer ATS
   - 样式：ATS优化
   - 适用：软件工程师、数据工程师

9. **lt-awesomecv** - Awesome CV
   - 样式：双栏、左侧强调
   - 适用：通用、工程师

10. **lt-deedy** - Deedy
    - 样式：双栏、高对比度
    - 适用：软件工程、学生

### 时间线/项目 (2个)

11. **jr-timeline** - Timeline
    - 样式：时间线布局
    - 适用：项目管理、咨询

12. **lt-twenty** - Twenty Seconds
    - 样式：单页时间线、视觉化
    - 适用：市场营销、产品经理

## 🎨 切换模版的核心原理

### 不会改变内容的原因

```typescript
// 用户切换模版时
onTemplateSelected(template: Template) {
  // 1. 只改变CSS变量
  const cssVars = tokensToCssVars(template.tokens);

  // cssVars 示例：
  // {
  //   '--font-family': 'Georgia',
  //   '--font-size': '14px',
  //   '--color-accent': '#2563eb',
  //   '--columns': '2',
  //   '--sidebar': 'left'
  // }

  // 2. 应用到DOM
  Object.entries(cssVars).forEach(([key, value]) => {
    element.style.setProperty(key, value);
  });

  // ❌ 完全不会修改简历内容HTML！
  // ✅ 简历数据 (personalDetails, experiences, education) 保持不变
  // ✅ 只是用不同的CSS样式来渲染相同的数据
}
```

### Token系统

每个模版都有一组Token定义其样式：

```typescript
interface TemplateTokens {
  fontFamily: string;        // 字体：'Georgia', 'Arial', 等
  fontSize: number;          // 字号：12-16pt
  lineHeight: number;        // 行高：1.2-1.8
  palette: string;           // 配色：'blue', 'green', 'rose'
  layout: {
    columns: 1 | 2;          // 单栏或双栏
    sidebar: 'left'|'right'; // 侧边栏位置
    headerAlign: 'left'|'center'|'right';
    sectionHeadingStyle: 'rule'|'caps'|'pill'|'bar';
  }
}
```

## 📁 文件结构

```
frontend/src/assets/templates/
├── index.json                    # 当前精选12个模版配置
├── index-full-30.json.backup     # 备份：原来的30个模版
├── index-curated-12.json         # 精选12个模版配置
├── README-CURATED-12.md          # 本文档
├── thumbnails/                   # 12个缩略图
│   ├── jr-modern.svg
│   ├── jr-elegant.svg
│   ├── jr-even.svg
│   ├── jr-compact.svg
│   ├── jr-timeline.svg
│   ├── jr-dev-ats.svg
│   ├── lt-awesomecv.svg
│   ├── lt-altacv.svg
│   ├── lt-moderncv.svg
│   ├── lt-friggeri.svg
│   ├── lt-deedy.svg
│   └── lt-twenty.svg
└── thumbnails-archived/          # 存档：18个未使用的缩略图
```

## 🔄 如何恢复30个模版

如果需要恢复所有30个模版：

```bash
cd frontend/src/assets/templates
cp index-full-30.json.backup index.json
mv thumbnails-archived/*.svg thumbnails/
```

## ✅ 优势

1. **更清晰的选择** - 12个vs30个，用户不会overwhelmed
2. **更快的加载** - 更少的缩略图文件
3. **更好的质量** - 只保留最专业、最常用的模版
4. **类似CVWizard** - 对标成功产品的设计

## 🎯 使用方式

在编辑器页面：
1. 点击顶部工具栏的 "Templates" 按钮
2. 横向滚动浏览12个模版缩略图
3. 点击任意模版应用
4. 简历内容保持不变，只有样式改变

## 📊 对比

| 项目 | CVWizard | AlignCV (精简前) | AlignCV (精简后) |
|------|----------|------------------|------------------|
| 模版数量 | 12 | 30 | 12 |
| 切换不改内容 | ✅ | ✅ | ✅ |
| 横向滚动选择器 | ✅ | ✅ | ✅ |
| 缩略图预览 | ✅ | ✅ | ✅ |
| Token样式系统 | ❓ | ✅ | ✅ |

---

**更新时间**: 2025-11-26
**版本**: v1.0-curated-12
